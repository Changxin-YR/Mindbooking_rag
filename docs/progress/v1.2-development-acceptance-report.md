# V1.2 本轮开发与验收报告

日期：2026-09-05  
仓库：`C:\Users\27363\Desktop\books`  
验收基线：`C:\Users\27363\Documents\Codex\2026-09-04\bnag\outputs\平台项目最终需求与技术设计规格说明书.md`，以及 `docs/business/V1.2-FINAL-MASTER-DEVELOPMENT-SPEC-V2.md` 和 `part-01.md` 至 `part-10.md`。

## 1. 已检查模块

- 基础工程、FastAPI、Nuxt/Vite 三端、Compose、CI。
- IAM、Reader/Writer/Staff Session、Bearer 主体绑定、撤销和离职失效。
- 实名门禁、作者归属、内容发布版本、审核调用、阅读进度、书架和 Reader 页面。
- Wallet lot、赠币/充值币消费顺序、来源追踪、退款源查询。
- SQL Commerce 订单、充值、支付回调幂等/冲突、退款源归属和失败回滚。
- Commerce/Payout Provider 状态机、重复交易号、终态防回退和 callback 时间归一化。
- Staff credential/session/device/MFA factor/role migration 与 Admin staff-only gate。
- AuthorCenter/AdminCenter handler 级 Staff 权限和跨作者资源归属。
- SearchPort 过滤、排序、纠错、降级，以及 Agent Gateway Tool/Resource 边界。
- OpenAPI 路由、Migration、Tests、Ruff、Mypy、前端测试/类型检查/构建。

## 2. 问题数量与分类

初始符合性审计记录：P0 7、P1 12、P2 3、P3 2。  
本轮重点关闭了认证主体/IDOR、充值实名门禁、退款认证与归属、审核归属、SQL Wallet 来源追踪、SQL Review/治理/Finance 适配、Staff Admin 会话隔离等问题；未将部分实现提升为完全符合。

当前仍保留的高风险差异：P0 级主要集中在全域持久化、跨域财务事务边界、完整收益/结算/提现策略和生产级 DataScope/MFA 流程。

## 3. 已修复与补全

- Reader、Writer、Admin 使用真实 Bearer 会话；Admin 不接受 Reader account session。
- 后端私有 API 校验 Session、主体类型和资源归属，避免仅依赖前端隐藏按钮。
- 充值前强制 active real-name subject；退款按支付/充值源反查 account 并校验请求主体。
- Writer 固定版本提交，Admin 审核调用 Review API，并校验作品所属作者。
- SQL Wallet 支持 lot 锁定、赠币优先、流水、LotAllocation、来源和退款批次回收。
- SQL Commerce 支持订单持久化、确定性幂等键、支付渠道事件去重/冲突、退款源和 Wallet 失败回滚；SQL Refund 持久化快照、请求、源锁和重复 reference 幂等。
- Staff Account 独立凭据、Session、设备、MFA factor、角色/权限 migration；Admin 入口只允许 Staff session。
- SearchPort 支持多字段查询、过滤、排序、纠错和不可用时降级。
- Agent Gateway 仅通过注册的 Tool/Resource 回调访问业务服务；高风险写入被拒绝并提供审计记录入口。
- Reader、Writer、Admin 页面补充登录态、加载/错误/空状态和真实 API 请求；移除固定 demo 身份。

## 4. 数据库修改

- `0015_auth_credentials`：账号密码凭据和可撤销 Session。
- `0016_wallet_lot_sources`：Wallet lot `source_ref`、`issued_amount` 和来源索引。
- `0017_staff_auth_rbac`：Staff credentials、sessions、devices、MFA factors、roles、role bindings 和 role permissions。
- 修复 `0010_reader_experience` 在 MySQL 8.4 上对 `TEXT` 设置 JSON 默认值导致的迁移中断；增加在线部分迁移恢复和 offline SQL 分支。
- 已重新生成 `services/backend/migration.sql`，离线升级链当前完整到 `0030_finance_constraints`。

## 5. API 与架构修改

- 增加/收紧 Reader、Writer、Admin Bearer 入口和主体校验。
- 增加 Staff 登录、权限 gate、Search API 和 Agent Gateway 结构化边界。
- 增加 SQL Wallet/Commerce/Refund/Reading/Library/Review/Community/Notification/Support/Approval/Author Finance adapter，保留 `PERSISTENCE_MODE=memory` 作为测试默认；上述适配器已通过显式 `PERSISTENCE_MODE=sql` 接入主应用，充值回调、退款资产恢复与钱包操作覆盖 SQLAlchemy connection 事务边界；内存实现仍保留为测试回退。
- SQL IAM 已接入 SQL runtime；MySQL 真实注册/登录/实名/重复实名冲突 smoke 通过。OpenSearch REST projection 已接入并保留不可用降级。
- SQL IAM 的实名姓名与证件字段已改为环境密钥 Fernet 加密，数据库仅保留 HMAC 指纹用于去重/额度控制。
- apps profile 已由 placeholder 替换为真实 Backend、Reader、Writer、Admin 镜像；统一 Nginx API 路由、CORS、容器 healthcheck 和 Compose browser smoke 已通过。
- 修复 MySQL `GET_LOCK` 名称长度导致实名流程 500 的缺陷，并补充空搜索结果不应误报降级的回归测试。
- AI/Agent 未获得数据库直连能力；当前仅能调用业务服务回调。

## 6. UI/UX 修改

- Reader：登录设置、书架、钱包、客服、阅读进度、未成年人保护和互动请求接入会话。
- Writer：会话化工作台、作品/审核/日历状态和固定版本提交反馈。
- Admin：Staff 登录入口、独立会话、退出登录和工作台状态。
- 三端保持现有 Design System 方向，未新增无需求的产品范围。

## 7. 使用的 Skills / Tools

- `amicro-design-system`：统一三端视觉 token、密度和组件边界。
- `dispatching-parallel-agents`、`subagent-driven-development`：并行拆分认证、搜索、Agent、Commerce 等独立工作流。
- `test-driven-development`、`verification-before-completion`：为高风险路径保留回归测试并在结论前重新执行质量门禁。
- 本地命令行、Alembic、pytest、Vitest、TypeScript、Nuxt/Vite、Ruff、Mypy 和 CI 配置检查。

## 8. 资料参考

本轮未引入外部运行时依赖或复制第三方业务代码；实现依据冻结需求、当前技术栈和已有 migration/测试约定。

## 9. 测试情况

- Unit/Integration：后端按测试文件分批全量 `274 passed`；会员 Router 参数冲突、SQL 会员支付/票券、AuthorCenter 幂等/权限、AdminCenter 权限、Agent/Search/TTS/Notification、沙盒出款 HTTP API 和 Provider 状态机专项测试通过。
- Backend quality：Ruff format check、Ruff lint、Mypy 全部通过；新增 OpenSearch REST mock 回归和 SQL 实名密文回归。
- Frontend：Admin、Reader、Writer 及 api-client Vitest 通过；workspace typecheck 通过。
- Build：Admin、Writer Vite build 和 Reader Nuxt production build 通过。
- Migration：`alembic upgrade head --sql` 已生成到 `0030_finance_constraints`；运行中的 MySQL 版本为 `0030_finance_constraints`。
- Foundation：`scripts/validate-foundation.ps1` 通过。
- Manual/runtime：Docker 基础设施及 apps profile 已重建并通过 healthcheck；MySQL 8.4 已真实升级到 `0030_finance_constraints`，MySQL-backed Author/Content/Commerce/Wallet/Membership smoke 已通过，后端重启后作者、内容、支付、钱包和会员订单事实仍存在；OpenSearch 索引和查询适配器已实测，Redis/ClickHouse/MinIO/RabbitMQ 可用；MySQL-backed FastAPI health、OpenAPI、私有 API `401`、注册/登录/实名、充值/支付回调/退款、会员订单/沙盒支付（SUCCESS/FAILED/延迟/重复）、票券入账、礼物幂等和 Staff-only 沙盒出款（成功/失败/处理中/拒绝/超时/重复）smoke 已通过；`SANDBOX_ALIPAY` + `SANDBOX_BANK` 命名渠道商业 E2E 已通过；`qa_smoke.py`、Compose、Reader、Writer 编辑器和 V1.2 浏览器 smoke 已通过。真实第三方支付、跨实例并发和完整业务 E2E 仍未验证。
- 已知告警：pytest/pytest-asyncio 存在弃用警告，无失败测试。

## 10. 剩余问题

- Risk、Operation、Admin Center、Reader Experience、Copyright、Legal、Governance 等域已在 SQL 模式接入适配器，但完整业务策略、跨域事务、审计和生产级 E2E 仍未完成；Review、Community、Notification、Support、Approval、Author Finance、Reading、Library、Commerce/Refund 的 SQL 接入同样不等于全域业务完成。
- SQL Wallet 与 SQL Commerce 的充值回调、退款资产恢复已覆盖 connection-scoped 事务边界；章节购买和其他跨域财务仍未完成统一 Unit of Work、完整政策和对账。
- 完整会员、票券、礼物、成长、合同、收益、结算、提现和风控工作流仍为部分实现。
- OpenSearch 投影/索引生命周期、Outbox/MQ 消费、Ranking/Recommendation 运行态尚未完成。
- Agent Audit 仍为内存审计 hook，完整 Business API 和持久化审计未完成。
- Admin 目前完成 Staff session 和粗粒度权限 gate；ASSIGNED/DEPARTMENT/CUSTOM DataScope、MFA 验证流程和细粒度资源授权未完成。
- Compose apps profile 已替换为真实后端与三端应用容器；仍缺生产镜像发布、真实第三方支付、跨实例并发和完整业务浏览器 E2E。
- 2026-09-05 修复一个会员后台运行态缺陷：会员 Router 的通用调用器参数与计划/礼物字段 `name` 冲突，后台创建计划/礼物会返回 `500`；修复为 `method_name` 并加入 API 回归测试。该缺陷已关闭。
- 2026-09-05 关闭 AuthorCenter/AdminCenter handler 级 Staff 权限缺口，并修复跨作者 funnel 写入；补充 `operation.write`、`governance.write`、`review.read`、`review.decide` 回归测试。
- 2026-09-05 修复支付/出款 Provider 回调状态机：允许 `PROCESSING` 仅前进到终态、禁止终态回退、复用同一交易号更新事件，并统一处理 naive callback 时间。
- 2026-09-05 修复审核员质量指标写接口误用只读权限的问题，改为 `review.decide`；独立挂载的 `auth_required=True` Admin/Author Center 若缺少 Staff 授权回调会明确返回配置错误，不再静默放行。

## 11. 待确认业务问题

冻结文档未能确定、且本轮未自行臆定的内容包括：生产支付渠道/银行与税务凭据、正式合同具体分成版本、提现审批阈值与风控策略、会员/礼物完整计费配置。当前使用的 `SANDBOX_CN_2026_V1` 仅用于本地/Staging 验收，不替代正式合同、税务或法律政策；这些正式决策仍阻塞商业财务生产上线结论。

## 12. 最终符合性结论

当前实现已完成一组可运行、真实前后端联通并有回归证据的 V1.2 基础闭环，且认证、Staff Admin 隔离、Author/Content 持久化、Wallet 来源追踪、SQL Commerce/Refund 幂等与共享事务等高风险边界已加强；但与最终需求文档仍存在完整会员与章节购买、财务政策与对账、部分领域 SQL 持久化、OpenSearch/MQ 生命周期、Agent 审计、细粒度授权和生产支付环境差异。

因此本轮结论为：**PARTIAL，不具备宣称 V1.2 商业上线完成的依据**。工作树中的改动已完成本地可验证范围，剩余项应在 MySQL/Redis/OpenSearch/Docker 运行条件和生产业务参数具备后继续推进。

## 2026-09-05 continuation

本次继续开发完成并验证：Staff TOTP MFA（恢复码、加密 Secret、启用/禁用审计和高风险部门强制门禁）、Review `SELF/ASSIGNED/ALL/GLOBAL` 后端 DataScope、DB Outbox Worker 与 delivery 去重、Sandbox Payment/Payout Provider 全流程，以及 SQL 合同→收益→结算→提现→风控/财务审批→Payout 闭环。补充修复了沙盒 Provider 跨实例默认事件 ID 冲突和核心商业事件未注册导致的 Outbox FAILED。

真实 SQL 商业 E2E 已通过：作者创建作品/章节→固定版本发布审核→合同审批生效→配置 VIP→Reader 实名充值→签名支付回调及重复回调→购买章节/权益→Wallet/Ledger→作者收益确认→Settlement→Withdrawal→Risk/Finance 审核→Sandbox Payout 成功及重复回调。数据库核验显示本轮 12 类核心事件全部 `PROCESSED`，当前失败数 `0`，delivery 无重复 `(event_id, consumer)` 键。

最新质量证据：后端新增政策/渠道回归后按测试文件分批 `274 passed`（Windows 单进程全量会触发 TestClient socket 资源上限；失败测试单独复跑通过）；Ruff、Ruff format、Mypy、workspace Vitest、typecheck、production build 全部通过；`qa_smoke.py`、`qa_compose_smoke.py`、`qa_v12_smoke.py`、`qa_writer_editor_smoke.py`、`qa_sql_commercial_e2e.py` 及会员/出款沙盒 SQL smoke 全部通过；运行中的 MySQL migration head 为 `0030_finance_constraints`，Outbox 当前统计为 `174 PROCESSED / 8 PUBLISHED`，delivery 重复键为 `0`。真实第三方支付/出款、全角色浏览器 E2E、跨实例并发和剩余非 SQL 领域仍未验证，因此结论继续保持 **PARTIAL**，不宣称生产上线完成。

## 2026-09-05 completeness continuation

本轮新增三项本地可验收闭环：

- Agent 审计：新增 Staff-only `GET /admin/api/v1/agent/audits` 显式 DTO，支持按 actor/tool/outcome 过滤分页；默认隐藏 arguments，敏感参数需 `agent.audit.sensitive` 二次授权；SQL sink 查询保持 append-only。
- Search Outbox：新增 `BOOK_INDEX`/`BOOK_TAKEN_DOWN` 事件处理器和事件 ID 幂等的本地投影存储；运行时 Worker 通过同一处理器更新投影并保留 delivery 去重。
- Reader TTS：新增统一 ContentAccessService 门禁的基础 TTS 元数据 API，支持固定男女声、限定倍速和句段时间轴；不会绕过 VIP/会员/购买权限。
- Notification：新增账户归属校验的通知历史查询 API，支持分类过滤、1-100 条限制、最新优先排序，并在 SQL 模式下从 `notifications` 表重建；未读状态和 Push/SMS 实际发送仍未完成。

同时增加 production fail-closed 配置校验：production 必须使用 SQL、强随机密钥和非 Sandbox provider；由于当前仓库仍未接入真实支付宝/微信/银行 Provider，production 配置会明确拒绝启动，不会静默使用沙盒实现。

新增验证：后端按测试文件分批全量 `274 passed`（含本轮新增 Agent/Search/TTS/Settings/Notification 回归），Ruff check、Ruff format、Mypy 通过；workspace Vitest、typecheck、build 通过。以上不改变全角色浏览器 E2E、跨实例并发、真实第三方支付/出款、税务法律和高级域的 `PARTIAL` 结论。

## 2026-09-05 恢复执行后的最终运行态

- 后端全量回归新鲜通过：`283 passed`；Ruff check、Ruff format、Mypy（143 个源文件）通过。
- 前端新鲜通过：workspace Vitest、`pnpm typecheck`、`pnpm -r build`；Reader、Writer、Admin 镜像已按当前工作树重建。
- Compose 全量服务健康；API `/health/ready` 与 Nginx `/healthz` 均返回 `200`；运行中 MySQL 8.4 的 Alembic head 为 `0031_finance_maker_checker`。
- 五组运行态验收全部通过：`qa_smoke.py`、`qa_compose_smoke.py`、`qa_v12_smoke.py`、`qa_writer_editor_smoke.py`、`qa_sql_commercial_e2e.py`。商业闭环使用独立 Risk/Finance Staff 审批者，支付和出款均通过命名沙盒 Provider，重复回调只产生一次业务结果。
- 当前可交付口径：可作为 SQL + 命名沙盒 Provider + `SANDBOX_CN_2026_V1` 虚拟合同税务策略的本地/Staging 验收版本部署。生产环境仍需真实 Provider 适配与凭据、正式法律/税务政策、跨实例并发和全角色浏览器 E2E；Coverage Matrix 中未验证项继续标记 `PARTIAL`。

## 2026-09-05 虚拟合同增量

- 合同创建会根据 `SANDBOX_CN_2026_V1` 自动生成确定性虚拟合同正文和 SHA-256，保存到 `contract_versions.document_text/document_hash`；Writer 签约页展示正文，`GET /writer/api/v1/finance/contracts/{contract_id}` 提供持久化读取。
- 新增 Alembic `0035_virtual_contract_document`，运行中的 MySQL 已从 `0034_notification_read_state` 升级到 `0035_virtual_contract_document`，不改变既有合同和财务流水事实。
- 新鲜验证：后端 `302 passed`；Ruff check/format、Mypy、workspace Vitest、`pnpm typecheck`、`pnpm -r build`、SQL 商业 E2E、MySQL 并发、Compose/V1.2/Writer smoke 均通过。
- 该合同正文和税务政策仍是沙盒演示材料，不构成正式法律合同、税务申报或生产结算依据。

## 2026-09-05 沙盒/E2E/压测继续开发

- 非财务第三方边界已统一为 SMS、OAuth、实名、内容审核、对象存储、通知的确定性沙盒 Provider；生产配置对 `SANDBOX_*` fail-closed。
- `python scripts/qa_role_e2e.py` 在重建后的 Compose 上返回 `status=PASS`，覆盖 11 个角色、预期越权拒绝、三端真实页面和 Super Admin Agent 成功调用。
- `python scripts/qa_advanced_workflows.py` 返回 `status=PASS`，覆盖 Review、Operation、Risk、Copyright、Legal、Governance、Agent 工作流，包含奖励幂等和参数 Maker/Checker 分离。
- `python scripts/qa_mysql_load.py --concurrency 4` 与 `python scripts/qa_mysql_concurrency.py --concurrency 8` 均返回 `status=PASS`；无负余额、无重复 Wallet 幂等键。结果仅为本地 Staging 证据，不代表生产容量。
- SQL 章节购买事务对 MySQL 1205/1213 死锁/锁等待失败增加最多 3 次完整事务重试，并保留唯一权益、追加账本和资金约束。
- 最近一次并发压测输出：20/20 成功，`35.03 rps`，p50 `0.0322s`，p95 `0.3557s`，最大 `0.3621s`，负余额 `0`，重复幂等键 `0`；该数据不构成生产容量承诺。

## 2026-09-05 恢复开发后的最终证据

- Alembic/MySQL head 已升级到 `0036_contract_author_signature`。虚拟合同保存 `signed_by`、`signed_at`、`signature_hash`，未签署不能激活；同一作者重复签署幂等，不同签署人冲突。
- 后端全量测试 `316 passed`；Ruff check/format、Mypy（144 源文件）、前端 Vitest、`pnpm typecheck`、`pnpm -r build` 全部通过。
- `qa_role_e2e.py` PASS，覆盖 11 个角色、三端真实页面、Agent 成功路径和越权拒绝。
- `qa_advanced_workflows.py` PASS，使用独立最小权限 Reviewer、Operator、Risk、Legal、Finance、Governance Maker/Checker、Agent token，覆盖 Review、Operation、Risk、Copyright、Legal、Governance、Agent，并验证跨域写入 `403`、Maker/Checker 和奖励幂等。
- `qa_mysql_concurrency.py --concurrency 8` PASS；`qa_mysql_load.py --concurrency 16` 与 `--concurrency 32` 均 PASS：16 路 112/112，p50 `0.1437s`、p95 `2.0691s`、吞吐 `34.42 rps`；32 路 224/224，p50 `0.2308s`、p95 `2.4243s`、吞吐 `37.67 rps`；工作负载包含登录、目录、Wallet 读取、支付回调和购买，两次均无负钱包和重复幂等键。
- 修复全局路由权限映射：发票使用 `finance.write`，Agent 资源/执行使用 `agent.execute`，Agent 审计使用 `agent.audit.read`，并保留 handler 层治理/敏感权限校验。

最终结论仍为 **PARTIAL**：当前可部署到本地/Staging 做完整沙盒验收；真实支付宝/微信/银行 Provider、正式税务/合同法律政策、跨实例生产容量、灾备监控和 Coverage Matrix 中未完成项仍不能宣称生产上线。

## 2026-09-05 fresh verification after resumed work

- 后端全量回归 `316 passed`；Ruff check、Ruff format、Mypy（144 个源文件）、workspace Vitest、`pnpm typecheck`、`pnpm -r build` 均通过。
- `qa_role_e2e.py` 返回 `status=PASS`，覆盖 Guest、Reader、Verified Reader、Author、Signed Author、Reviewer、Operator、Finance、Risk、Admin、Super Admin，并验证三端真实页面、越权拒绝和 Agent 成功路径。
- `qa_advanced_workflows.py` 返回 `status=PASS`，覆盖 Review、Operation、Risk、Copyright、Legal、Governance、Agent，包含最小权限、跨域 `403`、Maker/Checker 和奖励幂等。
- 串行 MySQL 压测：8 路回调 `PASS`；16 路 112/112，35.41 RPS，p50 `0.1351s`，p95 `1.9401s`，最大 `2.0307s`；32 路 224/224，37.08 RPS，p50 `0.2457s`，p95 `2.4265s`，最大 `3.8850s`；两轮负余额和重复 Wallet 幂等键均为 `0`。
- 修复对账 GET 集合路由权限映射，使中间件和 handler 一致使用 `governance.read`。

以上证据证明本地/Staging 沙盒闭环可回归，不升级生产结论。真实第三方凭据、正式税务/法律政策、多实例生产容量、灾备监控和 Coverage Matrix 中更宽的业务策略仍保持 `PARTIAL`。

## 2026-09-05 latest runtime pressure evidence

Backend 镜像包含章节购买的 Wallet-first 锁顺序和 MySQL 1205/1213 有界重试。重建后 16 路负载 112/112 成功（33.27 RPS，p50 `0.1289s`，p95 `2.1957s`，最大 `2.2477s`），32 路负载 224/224 成功（35.65 RPS，p50 `0.2714s`，p95 `2.4809s`，最大 `4.2433s`）；负余额、重复 Wallet 幂等键均为 `0`。该证据仅代表当前本地/Staging 环境，不升级生产容量结论。

最终 Backend 回归在上述代码基础上为 `319 passed`；旧段落中的 `316/318` 为历史快照，不能替代本段最终结果。

最终阈值压测门（p95 <= 5000ms，16 路吞吐下限 20 RPS、32 路吞吐下限 30 RPS）通过：16 路 112/112、32.41 RPS、p50 `0.1551s`、p95 `2.2376s`、最大 `2.3254s`；32 路 224/224、36.35 RPS、p50 `0.2646s`、p95 `2.4017s`、最大 `4.1414s`；负余额及重复 Wallet 幂等键均为 `0`。
